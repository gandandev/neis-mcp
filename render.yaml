services:
  - type: web
    name: neis-mcp
    runtime: python
    buildCommand: uv sync
    startCommand: uv run python src/server.py
    plan: free
    autoDeploy: true
    envVars:
      - key: ENVIRONMENT
        value: production
